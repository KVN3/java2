package main.handlers;

import database.Database;

public class ManagerHandler extends BaseHandler {
    public ManagerHandler(Database database) {
        super(database);
    }

    public void SaveReportsToFileSystem(){
        
    }
}
